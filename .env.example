# ArangoDB Configuration
ARANGO_ENDPOINT=http://localhost:8529
ARANGO_DATABASE=evidence_graph_dev
ARANGO_USERNAME=root
ARANGO_PASSWORD=dev

# PostgreSQL (for user auth only)
DATABASE_URL=ecto://postgres:postgres@localhost/evidence_graph_dev

# Phoenix Configuration
SECRET_KEY_BASE=<run: mix phx.gen.secret>
PHX_HOST=localhost
PORT=4000

# Optional: IPFS (Phase 2)
# IPFS_API_URL=http://localhost:5001

# Optional: Julia (Phase 2)
# JULIA_PATH=/usr/local/bin/julia

# Production Only
# POOL_SIZE=10
# ARANGO_POOL_SIZE=10
